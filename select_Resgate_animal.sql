# Qual a quantidade de animais resgatados = 25
SELECT COUNT(id_animal) FROM Animal;

# Qual a quantidade de doações = 10
SELECT COUNT(id_doacao) FROM Doacao;

# A porcentagem de animais adotador por ano
SELECT id_animal, data_adocao

# Quantos animais foram adotados
SELECT COUNT()